/**
 * Список доступных блоков: Heading, Question, Button, Footer.
 * Их можно перетаскивать (drag & drop) на Canvas.

 properties справа
 Если выбран Heading → текст, размер, стиль.
 Если выбран Question → текст вопроса, тип (single/multi/text), варианты ответов.
 Если выбран Button → текст кнопки, действие (Next/Submit).
 */
export const QuizBuilder = () => {
  return <div>QuizBuilder</div>;
};
